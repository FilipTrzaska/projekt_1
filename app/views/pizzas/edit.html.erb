<h1>Edit Pizza</h1>

<%= simple_form_for @pizza, :html => { :multipart => true } do |f| %>
    <%= f.select :category_id, @categories %>
    <%= f.input :title, label: "Pizza Name" %>
    <%= f.input :description %>
    <%= f.input  :author %>
    <%= f.button :submit %>
<% end %>
